# Pinned exact versions. Regenerate check: pip list --format=freeze
# Python 3.14 (native Windows build) — verified working combo on 2026-08-13.

fastapi==0.141.1
uvicorn[standard]==0.52.2
sqlalchemy==2.0.52
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained) reads bcrypt.__about__.__version__, which was
# removed in bcrypt>=4.1 — pin bcrypt to the last compatible release.
bcrypt==4.0.1
pillow==12.3.0
numpy==2.5.2
python-dotenv==1.2.2

# AI layer (ai/pipeline.py, imported by app/routers/reports.py) — ai/ is a
# sibling package at the repo root, not a backend subpackage, but its deps
# must live in this same venv since the backend imports it in-process.
# Canonical pins live in ai/requirements.txt; included here so
# `pip install -r backend/requirements.txt` alone produces a working env.
--extra-index-url https://download.pytorch.org/whl/cpu
-r ../ai/requirements.txt
